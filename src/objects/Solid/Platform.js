import { Solid } from "./Solid.js"

export class Platform extends Solid {
    constructor(
        x, y,
        width, height,
        color
    ) {
        super(x, y, width, height, color)
    }
}
